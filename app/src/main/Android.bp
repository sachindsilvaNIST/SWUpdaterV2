android_app {
    name : "SWUpdaterV2",
    sdk_version : "system_current",
    privileged : true,

    srcs: ["src/**/*.java"],

    static_libs : [
        "guava",
    ],

    optimize : {
        proguard_flags_files: [
            "proguard.flags",
        ],
    },
    resource_dirs: ["res"],
}
